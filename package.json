{
    "name": "cs2-signatures",
    "version": "1.0.0",
    "scripts": {
        "format": "prettier . --write",
        "generate": "git pull && npx tsx index.ts && git add . && git -c user.name='BOT' -c user.email='support@ianlucas.net' -c commit.gpgSign='false' commit -m 'chore: update readme' && git push"
    },
    "dependencies": {
        "@ianlucas/cs2-lib": "^5.2.0",
        "@ianlucas/depot-downloader": "^0.0.4"
    },
    "devDependencies": {
        "@types/node": "^22.3.0",
        "prettier": "^3.3.3"
    }
}
